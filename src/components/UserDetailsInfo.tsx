// src/components/UserDetailsInfo.tsx

import React from "react";
import { UserDetails as UserDetailsType } from "../types/types";

interface UserDetailsInfoProps {
  userDetail: UserDetailsType;
}

const UserDetailsInfo: React.FC<UserDetailsInfoProps> = ({ userDetail }) => {
  return <div></div>;
};

export default UserDetailsInfo;

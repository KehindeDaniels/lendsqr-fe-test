// src/components/GeneralDetails.tsx
import React from "react";
// import { useParams } from "react-router-dom";
// import { useUsers } from "../context/UserContext";

const GeneralDetails: React.FC = () => {
  //   const { userId } = useParams();
  //   const { userDetails } = useUsers();
  //   const userDetail = userDetails.find((user) => user.id === userId);

  return (
    <div>
      {/* <h3>General Details</h3>
      <p>Email: {userDetail?.personalInfo.email}</p>
      <p>Phone: {userDetail?.personalInfo.phone}</p>
      <p>Gender: {userDetail?.personalInfo.gender}</p> */}
      other general details
    </div>
  );
};

export default GeneralDetails;
